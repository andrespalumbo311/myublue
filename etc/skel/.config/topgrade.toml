# Topgrade configuration
# https://github.com/topgrade-rs/topgrade/blob/master/config.example.toml

[commands]
# Disable NPM updates because it's managed as a dependency of Homebrew
# and causes symlink conflicts (EEXIST) during global updates.
# Note: 'disable' expects a space-separated string, not a list.
disable = "npm"
